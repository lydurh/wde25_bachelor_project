/** Max driving distance (km) from admin origin before a location fee applies. */
export const BOOKING_MAX_DISTANCE_KM = 15;

/** Fixed location / travel fee in DKK when beyond {@link BOOKING_MAX_DISTANCE_KM}. */
export const BOOKING_LOCATION_FEE_KR = 200;
